<!DOCTYPE html>
<html>
<head>
</head>
<body>
<% flash.each do |type, msg| %>
  <div data-controller="flash" class="fixed top-4 right-4 z-50 p-4 rounded-md shadow-lg <%= type == 'alert' ? 'bg-red-50 text-red-700 border border-red-200' : 'bg-green-50 text-green-700 border border-green-200' %>">
    <%= msg %>
  </div>
<% end %>

<div class="w-full">
  <%= image_tag "home-logo.png", alt: "Track", class: "h-24 w-auto mb-8 mx-auto" %>

  <div class="flex flex-col items-center">
    <h1 class="text-2xl font-bold text-gray-900 mb-2">Sign in to Track</h1>

    <%= form_with url: start_bluesky_auth_path, method: :post, data: { turbo: false }, class: "w-full" do |f| %>
      <div class="mb-4">
        <%= f.text_field :handle, placeholder: "e.g. jay.bsky.social", required: true, autocomplete: "off", class: "appearance-none block w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm placeholder-gray-400 focus:outline-none focus:ring-blue-500 focus:border-blue-500 sm:text-sm text-center" %>
      </div>
      <%= f.submit "Connect with Bluesky", class: "w-full flex justify-center py-2 px-4 border border-transparent rounded-md shadow-sm text-sm font-medium text-white bg-[#287fc6] hover:bg-[#2069a5] focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-[#287fc6] cursor-pointer" %>
    <% end %>
  </div>

</div>

</body>
</html>
